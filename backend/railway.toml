[build]
builder = "NIXPACKS"

[deploy]
startCommand = "java -jar target/*.jar"
healthcheckPath = "/api/data/reports"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
